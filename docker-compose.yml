# SPDX-License-Identifier: AGPL-3.0-only
# SPDX-FileCopyrightText: 2025 Univention GmbH

services:
  udm-directory-connector:
    build:
      context: .
      dockerfile: docker/directory-importer/Dockerfile
      target: final
    image: artifacts.software-univention.de/nubus-dev/images/directory-importer:${IMAGE_TAG:-latest}
    read_only: true
    user: "1000:1000"
    cap_drop:
      - ALL
    volumes:
      - ./config/ad-domain-config.yaml:/etc/udm-directory-connector/config.yaml:ro
      #- ./config/ca_nubus.crt:/etc/ssl/certs/ca-certificates-nubus.crt:ro
      #- ./config/ca_ad.crt:/etc/ssl/certs/ca-certificates-ad.crt:ro
    environment:
      LOG_LEVEL: "INFO"
      CONFIG_FILENAME: "/etc/udm-directory-connector/config.yaml"
    network_mode: host
